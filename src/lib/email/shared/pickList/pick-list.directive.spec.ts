/* tslint:disable:no-unused-variable */

import { PickListDirective } from './pick-list.directive';

describe('Directive: PickList', () => {
  it('should create an instance', () => {
    let directive = new PickListDirective();
    expect(directive).toBeTruthy();
  });
});
