/* tslint:disable:no-unused-variable */

import { MomentFormatPipe } from './moment-format.pipe';

describe('Pipe: MomentFormat', () => {
  it('create an instance', () => {
    let pipe = new MomentFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
